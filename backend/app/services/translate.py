"""
Офлайн RU→EN перевод для EN-полосы калькулятора (§10 части 1) — намеренно без
внешних платных API, у проекта нет ключа Anthropic (см. CLAUDE.md).

argostranslate пробовали и убрали: транзитивно тянет полный PyTorch + CUDA
(через stanza), образ раздувается до ~8ГБ — на 0.5 vCPU / 512MB backend это
риск уронить OOM-killer'ом весь процесс при первом переводе, не только
скрыть EN-бар. Пока не найдена лёгкая офлайн-альтернатива без torch —
функция всегда возвращает None, calc.py и фронт уже это обрабатывают
(en_tokens = null → EN-полоса просто не рендерится).
"""
def translate_ru_to_en(text: str) -> str | None:
    return None
