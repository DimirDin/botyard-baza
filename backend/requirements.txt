fastapi==0.115.*
uvicorn[standard]==0.32.*
asyncpg==0.30.*
redis==5.2.*
pydantic-settings==2.6.*
httpx==0.27.*
tiktoken==0.8.*
# Офлайн RU->EN перевод для EN-полосы калькулятора — тяжёлая опциональная зависимость,
# модель ru->en (~200МБ) ставится через argospm при первом использовании. Если на проде
# не поднимется (0.5 vCPU / 512MB backend) — app/services/translate.py деградирует
# до отсутствия перевода, основной RU-подсчёт не затрагивается.
argostranslate==1.9.*
