---
slug: api-error-handling
title: "Обработка ошибок API и стратегия Retry"
summary: "Коды ошибок 400/401/429/529 и реализация экспоненциального backoff для стабильных интеграций."
section: theory
group: models
tags: [api, errors, reliability]
doc_url: "https://platform.claude.com"
sort_order: 70
published: true
---

![Error handling](/entry-images/api-request-response.jpg)

### ❓ Что это

Основные коды ошибок: `400` (невалидный запрос), `401` (неверный ключ), `403` (доступ запрещён),
`429` (rate limit), `500`/`529` (перегрузка Anthropic, временная). Разные коды требуют разной
реакции: `400` повторять бессмысленно без изменения запроса, `429`/`529` — кандидаты на retry с
backoff, `401` — немедленная остановка и алерт.

### 🎯 Зачем тебе

Без нормальной обработки ошибок продакшен рано или поздно ловит каскадный сбой: временная перегрузка
Anthropic при наивном коде превращается в видимый пользователю даунтайм, хотя проблема длилась
секунды.

Разные коды ошибок требуют не просто разной логики ретрая, но и разной реакции команды: `401` —
сигнал немедленно проверить конфигурацию (ключ отозван, истёк, неверно передан), это не то, что
самовосстановится ретраями, и алерт на него должен идти инженеру сразу. `429` — сигнал о том, что
собственный трафик превышает выделенный лимит, и решение — не бесконечный ретрай, а пересмотр rate
limiting на своей стороне или запрос повышения лимита. Смешивание всех ошибок в одну общую ветку
«попробовать ещё раз» маскирует эти принципиально разные сигналы друг под другом.

### 💻 Минимальный пример

```python
import time, random
RETRYABLE = {429, 500, 529}
def call_with_backoff(fn, max_attempts=5):
    for i in range(max_attempts):
        try:
            return fn()
        except ApiError as e:
            if e.status_code not in RETRYABLE or i == max_attempts - 1:
                raise
            time.sleep(min(2 ** i + random.random(), 30))
```

### ⚠️ Грабли

- **`400` почти никогда не решается повтором** — чини сам запрос, а не ретрай.
- **`529` — не твоя проблема с лимитами**, а перегрузка на стороне Anthropic, уместен терпеливый
  backoff.
- **Отсутствие таймаута на HTTP-клиенте** — запрос может висеть неопределённо долго без явного
  timeout.

### 🔗 Первоисточник
Error handling — platform.claude.com
