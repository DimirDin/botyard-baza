---
slug: api-prompt-caching
title: "Prompt Caching: снижение затрат до 90%"
summary: "Как размечать блоки контекста cache_control, чтобы не платить за повторное чтение больших кодовых баз."
section: theory
group: pricing
tags: [api, prompt-caching, cost]
doc_url: "https://platform.claude.com/docs/en/about-claude/pricing"
sort_order: 10
published: true
---

![Prompt caching](/entry-images/context-window.jpg)

### ❓ Что это

Prompt caching позволяет размечать блоки контекста (`cache_control: {"type": "ephemeral"}`), которые
Anthropic кэширует на своей стороне на ограниченное время. Повторное чтение закэшированного префикса
стоит до 90% дешевле обычного input-токена — модель не пересчитывает attention над тем же большим
системным промптом/кодовой базой при каждом запросе подряд.

### 🎯 Зачем тебе

Большой стабильный контекст, повторяющийся между запросами: system prompt с полной инструкцией
агента, вся кодовая база у coding-агента, длинная история в саппорт-боте. Без кэша каждый следующий
вопрос платит за полное чтение этого блока заново.

Это, вероятно, самый безрисковый и быстро окупающийся рычаг оптимизации стоимости из всех доступных
(см. чек-лист оптимизации costs) — в отличие от смены модели или изменения архитектуры пайплайна,
разметка `cache_control` не меняет качество ответов вообще, только то, платится ли полная цена за
уже виденный моделью контент. Для продуктов с большим system-промптом или agentic coding-инструментом,
где кодовая база перечитывается на каждый шаг, экономия легко достигает кратного снижения счёта за
input-токены без единого изменения в логике самого продукта.

### 💻 Минимальный пример

```json
{
  "system": [
    { "type": "text", "text": "Ты агент проекта botyard-baza...", "cache_control": { "type": "ephemeral" } }
  ],
  "messages": [...]
}
```

Кэшируемый блок должен быть **префиксом** — если что-то меняется до размеченного блока, кэш не
сработает.

### ⚠️ Грабли

- **Динамические данные в начале system-промпта убивают кэш для всего, что после них** — переноси
  их в конец, после закэшированного блока.
- **Кэш не бесплатен на запись** — первый запрос стоит чуть дороже, экономия со второго попадания.
- **TTL ограничен** — большая пауза между запросами снова платит полную цену.

### 🔗 Первоисточник
Prompt caching — platform.claude.com/docs/en/about-claude/pricing
