# Добавить в /etc/caddy/Caddyfile на сервере платформы (см. §1 MASTER_CONTEXT).
# Caddy сам выпустит и продлит сертификат Let's Encrypt для поддомена.

baza.botyard.site {
    reverse_proxy 127.0.0.1:3015
}
