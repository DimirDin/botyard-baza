# ГРАНДИОЗНОЕ ОБНОВЛЕНИЕ БАЗЫ — план партии 2026-08

Составлен 2026-08-20. Основан на фактическом состоянии `content/` (посчитано grep'ом,
не по BAZA_CONTEXT), карте меню `frontend/src/config/menu.js` и открытых хвостах из
BAZA_CONTEXT §15 / HANDOVER-baza.

---

## 0. РЕАЛЬНОЕ СОСТОЯНИЕ НА СЕЙЧАС (сверено с файлами)

| Сущность | Файлы | Опубликовано (прод, по HANDOVER) |
|---|---|---|
| Статьи `entries` | **121** md в 22 группах | 121 |
| Инструменты `tools.yaml` | **304** записи | 281 |
| Промпты `prompts.yaml` | **323** записи | 323 |
| Шпаргалки | **7** md (17–39 строк каждая) | 7 |
| Уроки Гида | **28** (4 уровня) | 28 |
| Компоненты CC (aitmpl) | seed на **122** из **1825** строк каталога | 122 |

### Три перекоса, которые видно невооружённым глазом

1. **Промпты: 232 из 323 (72%) сидят в одном табе `think`.** Восемь групп think —
   по 26–32 промпта каждая. Одновременно `ops/terraform`, `ops/ci-cd`,
   `content/translate`, `content/expand` — по **1 промпту**. Таб `code` — 27 на 5 групп.
   Пользователь, пришедший «за промптами для кода», видит почти пустые полки.
2. **Статьи: `theory/models` = 26 статей** (все `api-*` про вызовы API живут там),
   при том что 8 групп имеют по 2–4 статьи. Кандидат на разбиение уже записан
   в BAZA_CONTEXT §15.3 как открытый — до сих пор не сделан.
3. **Шпаргалки — 7 штук, самая длинная 39 строк.** Это самый дешёвый по
   производству и самый «сохраняемый» формат контента в мини-аппе, и он заброшен.
   Плюс все семь про Claude Code/API — ни одной про Claude.ai, ни одной про MCP-клиенты.

### Незакрытые хвосты, которые надо снять ДО новой партии

- `supabase-community/supabase-mcp` — строка живёт в прод-БД как `published=true`
  дубль, `sync_content.py` не умеет удалять. Ручной DELETE (команда в HANDOVER).
- `simonw/files-to-prompt` — `published=false` как stale, решить: вернуть или удалить.
- Контент **вшит в Docker-образ** (`COPY content ./content`): синк без `--build`
  молча заливает старое и рапортует успех. Это записано в памяти проекта — на этой
  партии обжечься нельзя, счётчики в выводе синка сверять глазами.

---

## 1. ЦЕЛЕВЫЕ ЦИФРЫ ПАРТИИ

| Сущность | Сейчас | Цель | Дельта |
|---|---|---|---|
| Статьи | 121 | **200** | +79 |
| Инструменты | 304 | **380** | +76 |
| Промпты | 323 | **430** | +107 (все вне `think`) |
| Шпаргалки | 7 | **24** | +17 |
| Уроки Гида | 28 | **40** (5-й уровень) | +12 |
| Компоненты CC | 122 | **300** | +178 |
| Группы меню | 22/25/26 | +6 новых групп | — |

Инвариант всей партии: **после неё ни одна группа любого раздела не имеет меньше
5 карточек.** Это единственный численный критерий приёмки.

---

## ФАЗА 1. СТРУКТУРА МЕНЮ (делать первой — иначе контенту некуда ложиться)

Правка `frontend/src/config/menu.js` + иконки `frontend/public/icons/`.
Помним багу из §7.2: категория, которой нет в меню, попадает в БД `published=true`
и физически недостижима в навигации. Поэтому меню — раньше контента, в том же PR.

### 1.1. Разбить `theory/models` (26 статей) на четыре группы

| Новая группа | Что переезжает | Примерно |
|---|---|---|
| `theory/models` (оставить) | `api-models-matrix`, `con-model-selection`, `api-fast-mode`, `api-service-tiers`, `api-rate-limits-tiers` | 5–7 |
| `theory/api-tools` (новая) | `api-bash-tool`, `api-code-execution-tool`, `api-computer-use`, `api-text-editor-tool`, `api-web-search-tool`, `api-web-fetch-tool`, `api-tool-search-tool`, `api-memory-tool`, `api-strict-tool-use`, `api-tool-use-json`, `api-parallel-tool-use`, `api-programmatic-tool-calling` | 12 |
| `theory/api-io` (новая) | `api-streaming-responses`, `api-fine-grained-streaming`, `api-structured-outputs`, `api-citations`, `api-files`, `api-pdf-support`, `api-vision-processing`, `api-error-handling`, `api-token-counting` | 9 |
| `theory/reasoning` (новая) | `api-extended-thinking`, `api-adaptive-thinking`, `con-think-tool`, `api-batch-processing` | 4 |

Миграция = правка поля `group:` во фронтматтере + новые записи в `BASE_MENU` + 3 иконки.
`sync_content.py` перезапишет строки по slug, данных не теряем. Избранное живёт по
entry_id — не ломается.

### 1.2. Новые группы под новый контент

- `base/code` → **`agent-sdk`** (Claude Agent SDK — 0 статей сейчас, тема большая)
- `base/chat` → **`cowork`** (Claude в desktop/Cowork-режиме, 0 статей)
- `base/theory` → **`evals`** (оценка качества, guardrails, регрессии — 0 статей)
- `prompts` → таб `think` **не расширять вообще** (он и так 72%)
- `tools/apps` → **`terminal`** (обвязки-терминалы, TUI, statusline — сейчас размазаны по `desktop`)

Итог: BASE_MENU 22 → 28 групп, TOOLS_MENU 26 → 27.

### 1.3. Иконки

6 новых PNG по пайплайну §13 (тёмно-оранжевый глиф на #16130F).
**Никаких копий-плейсхолдеров** — на них уже наступали дважды (`apps/editors`,
таб `think`, где до сих пор плейсхолдер из `steer/general.png`). Заодно закрыть
и старый долг по иконкам `think`: 9 глифов (таб + 8 групп).

---

## ФАЗА 2. СТАТЬИ: +79 (121 → 200)

### 2.1. Добивка тощих групп до 8 статей (+41)

| Группа | Сейчас | Добавить | Темы |
|---|---|---|---|
| `code/claude-md` | 3 | +5 | импорты `@file`, монорепо-иерархия, CLAUDE.md для команды, чем отличается от AGENTS.md, ревизия раздутого файла |
| `code/hooks` | 3 | +5 | матрица событий, exit-коды и блокировка, hooks + форматтеры, отладка молчащего хука, hooks vs skills |
| `code/permissions` | 3 | +5 | allow/deny/ask-правила, permissions в CI, `--dangerously-skip-permissions` и когда это оправдано, аудит разрешений, песочница vs permissions |
| `code/skills` | 5 | +3 | скилл с ресурсами, версионирование, скилл из существующего промпта |
| `code/mcp` | 6 | +2 | OAuth к remote MCP, дебаг MCP-соединения |
| `code/subagents` | 4 | +4 | стоимость сабагентов, когда НЕ делегировать, передача артефактов, параллель vs последовательность |
| `code/headless-ci` | 4 | +4 | стриминг JSON в CI, лимиты бюджета, self-hosted runner, secrets |
| `chat/artifacts` | 4 | +4 | артефакты с данными, публикация и приватность, диаграммы, ограничения CSP |
| `chat/memory` | 4 | +4 | память vs проекты в 2026, гигиена, отключение, память в командах |
| `chat/connectors` | 4 | +4 | каталог коннекторов, права и риск, коннекторы vs MCP на практике, корпоративные политики |
| `chat/research` | 4 | +4 | глубина vs скорость, цитаты и проверка, research в проектах, лимиты |
| `chat/projects` | 6 | +2 | инструкции проекта, объём знаний и деградация |
| `theory/tokenization` | 3 | +5 | кириллица в цифрах, токены кода, подсчёт до отправки, токены в изображениях, бюджет на диалог |
| `theory/context-window` | 5 | +3 | 1M-контекст на практике, компактация, attention decay в длинных сессиях |
| `design/*` (4 группы) | по 2 | +3 каждой = +12 | самая заброшенная секция: генерация UI, итерации, токены/системы, экспорт в код |

*(суммарно ~+66 позиций по строкам выше — приоритизировать до +41 по факту,
остаток уходит в следующую партию)*

### 2.2. Новые группы (+24)

- `code/agent-sdk` — 8 статей: что такое Agent SDK, отличие от Claude Code CLI,
  свой агент за 50 строк, tool runner, managed agents, стриминг, стоимость, продакшен-обвязка.
- `chat/cowork` — 8 статей: Cowork-режим, desktop-приложение, работа с файлами,
  скиллы в Cowork, ограничения, безопасность.
- `theory/evals` — 8 статей: зачем эвалы, LLM-as-judge, golden set, регрессии
  промптов, метрики, дрейф модели после апдейта, guardrails, безопасность промптов.

### 2.3. Формат — без отклонений

Пять заголовков (Что это → Зачем тебе → Минимальный пример → Грабли → Первоисточник),
40–85 строк, картинка из существующих 9 JPG, `doc_url` во фронтматтере обязателен.
Mermaid — точечно, в 6–8 самых схемных статьях (компонент `Mermaid.jsx` уже есть).

### 2.4. Верификация фактов — жёстко

Каждая статья сверяется с каноническим источником (§14): docs_site_map, claude_code_docs_map,
support.claude.com, pricing. **Ни одна цифра цены/лимита не пишется по памяти** —
только из `content/cheatsheets/api-limits-and-models.md` или свежей сверки с pricing-страницей.
Правило проекта, уже спасавшее от ошибок в стороннем дампе.

---

## ФАЗА 3. ПРОМПТЫ: +107, все мимо `think`

Цель — сломать перекос 72%. Ни одного нового промпта в `think`.

| Таб | Сейчас | Цель | Что писать |
|---|---|---|---|
| `code` | 27 | **75** | generate +10 (тесты, миграции схем, CLI, парсеры), refactor +10, debug +12 (сейчас **1**!), migrate +10, architect +9 |
| `review` | 20 | **50** | pr-review +10 (сейчас 2), logic +10 (сейчас 2), security +8, performance +7, style +5 |
| `ops` | 15 | **45** | ci-cd +12 (сейчас 1), terraform +9 (сейчас 1), k8s +7, docker +5, bash +5 |
| `docs` | 16 | **40** | api-docs +8, readme +7, comments +5, changelog +5, onboarding +5 |
| `content` | 12 | **35** | translate +8 (сейчас 1), expand +7 (сейчас 1), rewrite +5, compress +4, structure +4 |
| `steer` | 5 | **20** | +15: коррекция агента на середине, откат, сужение объёма, «не переписывай всё», выход из петли |
| `think` | 232 | **232** | **заморожен** |

Итого 430, доля `think` падает с 72% до 54%.

**Ребаланс:** `devops-ansible-hardening` переехать из `ops/terraform` в `ops/bash`
(зафиксировано как известный косяк в §7.3).

**Плейсхолдеры:** все новые промпты пишутся с переменными `{код}`, `{язык}`, `{стек}`
единообразно — это подготовка к «интерактивному конструктору переменных»,
который стоит первым пунктом в векторе развития HANDOVER.

---

## ФАЗА 4. ИНСТРУМЕНТЫ: +76 (304 → 380)

### 4.1. Правило добавления (не нарушать)

Каждый репозиторий **до** попадания в yaml проверяется через GitHub Search API:
существование, `full_name` (не переехал ли), `archived`, `pushed_at` < 12 мес, дедуп
grep'ом по `tools.yaml`. Партия 06.08 так дала 41 из 41 без единого 404 — паттерн рабочий.
После `sync_content.py` — **обязательно** `sync_github_stars.py`, иначе новые записи
остаются `published=false` и невидимы.

### 4.2. Куда добирать (самые тощие группы)

| Группа | Сейчас | Добавить |
|---|---|---|
| `apps/web` | 6 | +6 |
| `agents/orchestration` | 6 | +6 |
| `agents/autonomous` | 6 | +6 |
| `agents/subagents` | 7 | +5 |
| `apps/vscode` | 7 | +5 |
| `code/hooks` | 7 | +5 |
| `agents/workflow` | 8 | +4 |
| `code/rules` | 8 | +4 |
| `mcp/clients` | 8 | +4 |
| `libs/other`, `code/ci-cd`, `apps/mobile`, `mcp/docker` | 9 | +3 каждой |
| `libs/rust`, `libs/go` | 10–11 | +3 каждой |
| `apps/terminal` (новая) | 0 | +8 |
| Остальное (`mcp/servers` и пр.) | — | добор по качеству, не по количеству |

### 4.3. Гигиена каталога (в ту же партию)

- DELETE `supabase-community/supabase-mcp` из прод-БД (команда в HANDOVER).
- Решить судьбу `simonw/files-to-prompt`.
- Прогнать весь `tools.yaml` через GitHub API целиком — найти те, кто за год
  переехал/заархивировался с прошлых партий. Ожидаемо 5–15 находок.
- **Дописать `sync_content.py` поддержку удалений** (см. Фазу 7) — иначе такие
  хвосты будут копиться каждую партию, это уже третий случай.

---

## ФАЗА 5. ШПАРГАЛКИ: 7 → 24 (самый недооценённый раздел)

Формат — плотная таблица, 20–40 строк, с датой сверки в первой строке
(как в `cc-cli-flags.md`: «Сверено с …, 04.07.2026»). Дёшево в производстве,
максимально «сохраняемо» пользователем.

**Claude Code (+6):** subagent-фронтматтер · hooks-события и exit-коды ·
skills-структура `SKILL.md` · plugin/marketplace манифест · settings.json целиком ·
шпаргалка по `/`-командам, разбитая по частоте.

**API (+5):** матрица моделей и цен (вынести из `api-limits-and-models`) ·
параметры Messages API · tool-use JSON-схема · коды ошибок и ретраи ·
prompt caching: что кэшируется и на сколько.

**Claude.ai (+4) — сейчас ноль:** горячие клавиши веба · лимиты по планам ·
Projects: что где лежит · Connectors: права по каждому.

**MCP (+2):** конфиг клиента (Claude Code / Desktop / IDE) · транспорты stdio/SSE/HTTP.

⚠️ Раздел плоский (без табов). На 24 карточках плоский список станет неудобным —
Фаза 7 добавляет группировку по `category` (поле уже есть во фронтматтере, просто не используется UI).

---

## ФАЗА 6. ГИД: 5-й уровень, +12 уроков (28 → 40)

Четыре уровня закрывают путь до «агентных паттернов». Уровень 5 — **«Продакшен»**,
для тех, кто прошёл всё:

1. Свой MCP-сервер за вечер
2. Claude Agent SDK: первый агент
3. Хуки как контроль качества
4. Плагины и маркетплейс команды
5. Headless в GitHub Actions
6. Бюджет и стоимость агента
7. Эвалы: как понять, что стало хуже
8. Песочница и безопасность
9. Мульти-агент: когда оправдан
10. Долгоживущие сессии и компактация
11. Отладка агента, который «залип»
12. Чек-лист выката агента в прод

Требования те же, что у уровней 1–4 после редизайна (§7.5.1–7.5.4): JPG-иллюстрация,
секция «Смотри в приложении» с **реальными** slug'ами из `content/entries` и
`tools.yaml` (не выдуманными — на этом уже обжигались), `related_entry`.

---

## ФАЗА 7. КОД: 6 задач, без которых партия развалится

| # | Задача | Почему сейчас |
|---|---|---|
| 7.1 | **`sync_content.py`: удаление осиротевших строк** — режим `--prune`, удаляющий из БД записи, которых нет в файлах | третий раз подряд руками чистим прод-БД. Корень проблемы |
| 7.2 | **Валидация `category` против `menu.js`** на этапе синка, падать с ошибкой | категория не из меню = контент в БД, недостижимый в UI. Баг повторялся дважды |
| 7.3 | **Группировка шпаргалок** по `category` (поле есть, UI игнорирует) | 24 карточки плоским списком нечитаемы |
| 7.4 | **Cron для `sync_github_stars.py`** на VPS (еженедельно) | звёзды и «инструменты недели» протухают; записано в векторе HANDOVER |
| 7.5 | **Конструктор переменных в промптах** — подстановка `{код}`/`{язык}`/`{стек}` перед копированием | первый пункт вектора развития; +107 новых промптов пишутся под него |
| 7.6 | **Защита от «тихого синка»** — `sync_content.py` печатает git-hash вшитого контента и сравнивает с текущим HEAD | контент вшит в образ, синк без `--build` рапортует успех на старых данных |

Плюс, если останется бюджет: `entry_ratings` есть только у статей — распространить
👍/👎 на инструменты и промпты (даёт сигнал, что чистить в следующую партию).

---

## ФАЗА 8. КОМПОНЕНТЫ CC: 122 → 300

`content/components/aitmpl_catalog.csv` — 1825 строк, засеяно 122. Пайплайн
(`fetch_bodies.py` → `make_seed.py` → `cc_components_seed.sql`) уже написан
и работает. Задача чисто объёмная: отобрать ещё ~178 по покрытию 26 групп
`COMPONENTS_MENU`, прогнать `fetch_bodies.py`, пересобрать seed.
Приоритет — ниже фаз 1–7, это «догрузить существующим конвейером».

---

## ПОРЯДОК ИСПОЛНЕНИЯ

```
Фаза 1 (меню + иконки)          ─┐
Фаза 7.1/7.2 (синк: prune+валидация) ─┴─ строго первыми, всё остальное на них опирается
        │
        ├─ Фаза 2 (статьи +79)      ─┐
        ├─ Фаза 3 (промпты +107)    ─┤ параллельно, независимы
        ├─ Фаза 4 (инструменты +76) ─┤
        ├─ Фаза 5 (шпаргалки +17)   ─┘
        │
        ├─ Фаза 6 (Гид, уровень 5) ── после Фазы 2: ссылается на новые статьи
        ├─ Фаза 7.3–7.6 (UI/инфра)
        └─ Фаза 8 (компоненты)
```

Резать при нехватке времени — с конца: Фаза 8, затем 6, затем 5. **Фазы 1 и 7.1/7.2
не режутся ни при каких условиях** — без них партия создаёт новые невидимые записи
и новые осиротевшие строки.

---

## ПРИЁМКА ПАРТИИ

1. `grep -c` по файлам совпадает с целевыми цифрами раздела 1.
2. Ни одна группа любого раздела не имеет < 5 карточек.
3. Каждая новая категория присутствует в `menu.js` **и** имеет собственную иконку.
4. `sync_content.py` отработал на **пересобранном** образе, счётчики в выводе
   сверены глазами с локальным `grep -c` (защита от тихого синка).
5. `sync_github_stars.py` прогнан после синка, ни одного нового `published=false`.
6. `/api/gate/check` проверен вручную — гейт не тронут.
7. 32/32 бэкенд-теста проходят; `npm run lint && npm test && npm run build` зелёные.
8. Живая проверка в реальном Telegram: новая группа, новая шпаргалка, новый урок
   уровня 5, промпт с плейсхолдерами.
9. `BAZA_CONTEXT.md` §7/§15/§17 обновлён — **реальными** цифрами из файлов,
   а не целевыми из этого плана.

---

## РИСКИ

| Риск | Митигация |
|---|---|
| Тихий синк старого контента (вшит в образ) | Фаза 7.6 + обязательный `--build`, сверка счётчиков |
| Категория не в меню → невидимый контент | Фаза 7.2 — падать на синке, а не в проде |
| Осиротевшие строки в БД | Фаза 7.1 `--prune` |
| Галлюцинация репозиториев | Поштучная проверка через GitHub API до записи в yaml (правило §14) |
| Устаревшие цены/лимиты | Только из `api-limits-and-models.md` / сверка с pricing-страницей |
| Разбиение `theory/models` ломает ссылки | Slug'и не меняются, только `group:` — избранное и диплинки по id живы |
| Объём партии → выгорание качества | Формат статьи фиксирован, шпаргалки как дешёвый формат-разгрузка |
