# Только для `npm run dev` — Vite не подхватывает этот файл в `vite build`.
# Включает лёгкую имитацию backend (src/lib/mock.js), чтобы смотреть экраны
# без реального Telegram initData (его нельзя подделать без бота).
VITE_USE_MOCK=1
